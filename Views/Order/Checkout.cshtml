@{
    ViewData["Title"] = "Thanh toán";
    var user = ViewBag.User as QLNhaSach1.Models.User;
    var cart = ViewBag.Cart as QLNhaSach1.Models.Cart;
}

<h2>Thông tin thanh toán</h2>
<!-- Form đặt hàng -->
<form method="post" asp-action="Checkout">
    <div class="mb-3">
        <label class="form-label">Họ tên:</label>
        <input name="userName" class="form-control" value="@user?.UserName" />
    </div>
    <div class="mb-3">
        <label class="form-label">Email:</label>
        <input name="email" class="form-control" value="@user?.Email" />
    </div>
    <div class="mb-3">
        <label class="form-label">Số điện thoại:</label>
        <input name="phone" class="form-control" value="@user?.Phone" />
    </div>
    <div class="mb-3">
        <label class="form-label">Địa chỉ:</label>
        <input name="address" class="form-control"
            value="@(user?.Address != null && user.Address.Any() ? user.Address.First() : "")" />
    </div>

    <h4>Đơn hàng của bạn:</h4>
    @if (ViewBag.DiscountAmount != null && (decimal)ViewBag.DiscountAmount > 0)
    {
        <li class="list-group-item d-flex justify-content-between text-danger">
            <span>Giảm giá</span>
            <span>-@(((decimal)ViewBag.DiscountAmount).ToString("N0")) VNĐ</span>
        </li>
        <li class="list-group-item d-flex justify-content-between fw-bold">
            <span>Tổng sau giảm</span>
            <span>@(((decimal)ViewBag.TotalAfterDiscount).ToString("N0")) VNĐ</span>
        </li>
    }
    else
    {
        <li class="list-group-item d-flex justify-content-between fw-bold">
            <span>Tổng cộng</span>
            <span>@cart.TotalPrice.ToString("N0") VNĐ</span>
        </li>
    }

    <div class="mb-3">
        <label class="form-label">Phương thức thanh toán:</label>
        <select name="paymentMethod" class="form-select">
            <option value="COD">Thanh toán khi nhận hàng</option>
            <option value="PayPal">PayPal</option>
        </select>
    </div>

    <button type="submit" class="btn btn-primary">Đặt hàng</button>
</form>


<form method="post" asp-action="ApplyDiscount" class="mb-4">
    <div class="mb-3">
        <label class="form-label">Mã giảm giá (nếu có):</label>
        <input name="discountCode" class="form-control" placeholder="Nhập mã..." value="@ViewBag.DiscountCode" />
    </div>
    <button type="submit" class="btn btn-outline-primary">Áp dụng</button>
</form>