@model QLNhaSach1.Models.Book

@{
    ViewData["Title"] = "Thêm Sách Mới";
}

<h2 class="mb-4 text-primary">@ViewData["Title"]</h2>

<form asp-action="Create" method="post" class="needs-validation" novalidate>
    @Html.ValidationSummary(true, "", new { @class = "alert alert-danger" })

    <div class="row mb-3">
        <div class="col-md-6">
            <label asp-for="bookName" class="form-label">Tên sách</label>
            <input asp-for="bookName" class="form-control" />
            <span asp-validation-for="bookName" class="text-danger"></span>
        </div>

        <div class="col-md-6">
            <label asp-for="author" class="form-label">Tác giả</label>
            <input asp-for="author" class="form-control" />
            <span asp-validation-for="author" class="text-danger"></span>
        </div>
    </div>

    <div class="mb-3">
        <label asp-for="description" class="form-label">Mô tả</label>
        <textarea asp-for="description" class="form-control" rows="3"></textarea>
        <span asp-validation-for="description" class="text-danger"></span>
    </div>

    <div class="row mb-3">
        <div class="col-md-4">
            <label asp-for="quantity" class="form-label">Số lượng</label>
            <input asp-for="quantity" class="form-control" type="number" min="1" />
            <span asp-validation-for="quantity" class="text-danger"></span>
        </div>

        <div class="col-md-4">
            <label asp-for="price" class="form-label">Giá (VNĐ)</label>
            <input asp-for="price" class="form-control" type="number" min="0" step="1000" />
            <span asp-validation-for="price" class="text-danger"></span>
        </div>

        <div class="col-md-4">
            <label asp-for="bookStatus" class="form-label d-block">Tình trạng</label>
            <div class="form-check">
                <input asp-for="bookStatus" class="form-check-input" type="checkbox" />
                <label asp-for="bookStatus" class="form-check-label">Còn hàng</label>
            </div>
            <span asp-validation-for="bookStatus" class="text-danger"></span>
        </div>
    </div>

    <div class="mb-3">
        <label asp-for="CategoryId" class="form-label">Danh mục</label>
        <select asp-for="CategoryId" class="form-select" asp-items="ViewBag.Categories">
            <option value="">-- Chọn danh mục --</option>
        </select>
        <span asp-validation-for="CategoryId" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="ImageUrl" class="form-label">URL hình ảnh</label>
        <input asp-for="ImageUrl" class="form-control" />
        <span asp-validation-for="ImageUrl" class="text-danger"></span>
    </div>

    <div class="d-flex justify-content-start mt-4">
        <button type="submit" class="btn btn-primary me-2">Thêm sách</button>
        <a asp-action="Index" class="btn btn-secondary">Quay lại</a>
    </div>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
